# This folder is for files and scripts related to Openshift 4 HA Cluster
# v0.001 20200103
# This calls upon two major files /os2 for staging (dev/test) and os (production) 
# Within each folder is inventory.yml file which lists host names in stanzas of groups. 